storeFile=upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=

# Local one-time generation (do not commit the jks):
# keytool -genkeypair -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 \
#   -validity 10000 -alias upload
# Then copy this file to keystore.properties and fill passwords.
# After the first Play App Signing enrollment, add BOTH upload-key and
# Play App Signing SHA-1/SHA-256 to the Google OAuth Android client
# for package io.rank5.app.
